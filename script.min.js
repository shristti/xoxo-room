const SECRET_PASSWORD="ifoyou";const form=document.getElementById("lockForm");const passwordInput=document.getElementById("passwordInput");const errorBox=document.getElementById("lockError");if(localStorage.getItem("xoxoRoomUnlocked")==="true"){document.body.classList.remove("locked");document.body.classList.add("unlocked");}form.addEventListener("submit",function(e){e.preventDefault();const value=passwordInput.value.trim();if(!value){errorBox.textContent="whisper something at least;)";return;}if(value===SECRET_PASSWORD){document.body.classList.remove("locked");document.body.classList.add("unlocked");errorBox.textContent="";localStorage.setItem("xoxoRoomUnlocked","true");}else{errorBox.textContent="mmm,that’s not it. try again,lover.";passwordInput.value="";}});document.querySelectorAll("nav a[data-scroll]").forEach((link)=>{link.addEventListener("click",(e)=>{e.preventDefault();const target=document.querySelector(link.getAttribute("data-scroll"));if(!target)return;target.scrollIntoView({behavior:"smooth",block:"start"});});});const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting){entry.target.classList.add("visible");}});},{threshold:0.18});document.querySelectorAll("main section").forEach((section)=>{observer.observe(section);});